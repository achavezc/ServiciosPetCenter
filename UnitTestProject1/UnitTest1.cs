using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace UnitTestProject1
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
        }

        public void YipiId_TwoDifferentTimes_SecondTimeKeyPrecedesTheFirstTimeKey()
        {
            //// Arrange
            //DateTime firstTime = new DateTime(2013, 12, 18, 11, 00, 00);
            //DateTime secondTime = new DateTime(2013, 12, 18, 11, 00, 01);
            //YipiKeys keyGenerator = YipiKeys.getYipiKeys();

            //// Act
            //string firstYipiKey = keyGenerator.YipiId(firstTime);
            //string secondYipiKey = keyGenerator.YipiId(secondTime);

            //// Assert
            //Assert.IsTrue(secondYipiKey.CompareTo(firstYipiKey) < 0);
        } 
    }
}
